<div class="<%= flash_alert_class(type) %> text-white px-6 py-4 border-0 rounded relative mb-4 flex flex-row items-center justify-between shadow-lg transform transition-all duration-300 ease-in-out" 
      role="alert" 
      data-controller="flash">
  <span class="inline-block align-middle mr-8">
    <%= message %>
  </span>
  <button class="absolute bg-transparent text-2xl font-semibold leading-none right-0 top-0 mt-4 mr-6 outline-none focus:outline-none" 
          data-action="click->flash#close">
    <span>×</span>
  </button>
</div>
